-- 코드를 입력하세요
SELECT NAME, count
    FROM (
        SELECT NAME, count(*) as count
        FROM ANIMAL_INS
        WHERE NAME <> 'NULL'
        GROUP BY NAME
    ) a
    WHERE count >= 2
    ORDER BY NAME;